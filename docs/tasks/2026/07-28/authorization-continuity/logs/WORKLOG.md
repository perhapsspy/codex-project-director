# 작업 기록

**2026-07-28**

- 실제 반복 실패와 사용자 교정을 현재 승인·검증 계약에 대조했다. broad `NEEDS_DECISION` 문구와 반대 방향 규칙의 부재가 수정된 새 판본을 새 권한으로 오해하게 만들 수 있음을 확인하고, 승인 기록 비교·무변경 재시도 의미·작업자 보고 재분류·사용자 질문 전 필드 변화 증명을 한영 후보와 설계 방향에 반영했다.
- 네 개의 맥락 비상속 회귀 시나리오를 실행했다. 국소 구문 오류를 인과적으로 수정한 새 판본은 `RUNNING`·무질문·같은 담당자 대표 검증으로, 작업자의 `NEEDS_DECISION` 종료 문구도 국소 실행으로 재분류됐다. 같은 판본·가정·입력의 `retry0` 반복은 사용자 질문 없이 금지하고 원인 수정으로 반환했으며, 격리 실행에서 사용자 활성 `WinSta0` 입력 주입으로 바뀌는 경우는 `Surface`·`Effect` 변화를 밝힌 `NEEDS_DECISION`으로 분류됐다.
